# 改造方案：claude-royale → 3 路波次防守

> 目标游戏：同学局像素防守联机（需求 V2.3）
> 底座：E:\dev\claude-royale（MIT，Phaser3 + React + Colyseus + TS）

## 一、架构结论（已摸清）

底座是**确定性模拟**架构，`shared/src` 是客户端与服务端共用的纯函数式逻辑层：

```
shared/src/
  constants.ts   全部参数（网格 32×18、时间、圣水、塔数据）
  projection.ts  2.5D 投影（左=顶，右=底，梯形）
  types.ts       SimState / SimEntity / PlayerSim 类型
  sim/state.ts   状态创建 + 唯一输入入口 playCard()
  sim/step.ts    每 tick 模拟主循环（732 行：寻路、攻击、弹道、塔）
  cards.ts       49 张卡牌定义（组件化）
server/src/BattleRoom.ts  房间（联机）
client/src/game/BattleScene.ts   Phaser 渲染主场景
client/src/game/arena.ts         竞技场绘制
client/src/i18n/                 有 en.ts + pt-BR.ts，可加 zh.ts
```

**核心优势**：改 `shared/sim` 一处，客户端与服务端同时生效，不会两边不一致。这就是为什么选它做底座。

## 二、概念映射（Clash Royale 零件 → 我们的机制）

| 现有（CR） | 我们（3路防守） | 改哪 |
|-----------|----------------|------|
| 左右两个玩家对攻（left/right） | 防守方 vs 怪物潮（mob side） | types.ts、state.ts |
| 3 塔：1 king + 2 princess | 1 核心 + 9 点位（3 路 × 3 点位） | constants.ts、state.ts |
| 出卡牌 playCard() | 英雄站点位 + 波次自动刷怪 + 三选一 | state.ts、step.ts |
| 圣水 elixir | 裂隙能量（对攻投怪用） | constants.ts |
| 49 张卡 | 12 张怪物卡 + 6 张英雄/技能卡 | cards.ts |
| 180 秒 + 突然死亡 | 8 波 ≈ 2–3 分钟 | constants.ts、step.ts |
| 2.5D 梯形竞技场 | 3 条横向进攻路线 | projection.ts、arena.ts |
| 手牌栏（底部） | 波次条 + 核心血条 + 点位 | ui/*、BattleScene.ts |
| 葡萄牙语 pt-BR | 中文 zh | i18n/ |

## 三、关键改造点（文件级）

### M0 状态层（先做，跑通单机防守）

| 文件 | 改动 |
|------|------|
| `types.ts` | 新增 `MobSide`；`SimEntity` 加 `lane`、`pointIndex`；新增 `Wave` 类型、`core` 字段、`element`（五行）字段 |
| `constants.ts` | 3 路定义（LANE_YS = [4, 9, 14]）、每路 3 点位、核心参数、波次表（WAVES）、单局时长 |
| `sim/state.ts` | `createInitialState()` 改为：1 核心 + 9 点位 + 波次状态；去掉双玩家卡牌手牌 |
| `sim/step.ts` | 新增 `stepWave()`（刷怪调度）；怪物寻路目标改为"最近点位→核心"；点位失守后退逻辑 |
| `cards.ts` | 精简为怪物卡（哥布林/兽人/寒冰骷髅/树人/恶魔）+ 少量技能卡；每卡带 `element` |

### M1 渲染层

| 文件 | 改动 |
|------|------|
| `projection.ts` | 保留 2.5D 梯形（复用透视感），把 3 路画成 3 条纵向走廊 |
| `arena.ts` | 画 3 路 + 9 点位 + 核心，去掉"河/桥" |
| `BattleScene.ts` | 去掉出牌拖拽，加波次条、核心血条、点位高亮 |
| `ui/*` | 删掉 CardHand/Deck/Collection 等卡牌界面，换防守 HUD |

### M2 联机与对攻

多人占位同步（点位归属）、裂隙能量、投怪、异步幽灵（AI 代守）。

### M3 英雄与五行

5 英雄像素化（换 Tiny Swords 素材）、五行相克（×1.25/×0.8）与相生（+15%/三连+30%）加成、总加成封顶 ×2.0、中文 zh.ts。

## 四、实施顺序与验证点

| 阶段 | 产出 | 怎么验证 |
|------|------|---------|
| M0 | 单机跑通：怪物一波波冲向核心，点位会失守后退 | `pnpm dev` 后单机能看到 |
| M1 | 3 路画面 + 波次条 | 截图对比，手机看 |
| M2 | 多人占位 + 互投 | 两个标签/两台手机 |
| M3 | 英雄 + 五行 + 中文 | 完整一局 |

## 五、风险与对策

| 风险 | 对策 |
|------|------|
| step.ts 732 行耦合深，改坏寻路 | M0 只加不改删：保留现有 unit 移动/攻击，先复用"塔=点位"完成防守闭环 |
| 确定性模拟要求逻辑纯净 | 新逻辑全部放 shared/sim，禁止引入随机外的副作用 |
| 中文化工作量大 | i18n 已分文件，先加 zh.ts 只覆盖主界面，游戏内文案后续补 |
| 一次改太多跑不起来 | 严格按阶段，每阶段改完跑 `pnpm dev` 验证再进下一步 |

## 六、M0 第一步具体改法（当前开始）

1. `constants.ts`：新增 3 路、9 点位、核心、波次参数（保留现有网格 32×18 复用）
2. `types.ts`：新增字段与类型
3. `sim/state.ts`：改 `createInitialState()` 生成核心 + 9 点位
4. `sim/step.ts`：加 `stepWave()` 刷怪，怪物出生在进攻侧、冲向核心
