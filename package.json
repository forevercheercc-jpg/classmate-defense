{
  "name": "claude-royale",
  "private": true,
  "version": "0.2.0",
  "packageManager": "pnpm@9.15.9",
  "description": "Claude Royale — jogo de estudo estilo Clash Royale no navegador (multiplayer em tempo real)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"pnpm --filter @claude-royale/server dev\" \"pnpm --filter @claude-royale/client dev\"",
    "test": "pnpm --filter @claude-royale/shared test",
    "build": "pnpm -r build",
    "deploy:client": "pnpm --filter @claude-royale/client build && npx wrangler pages deploy client/dist --project-name claude-royale --branch master"
  },
  "pnpm": {
    "overrides": {
      "@colyseus/core": "^0.16.24"
    }
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gifenc": "^1.0.3",
    "playwright": "^1.61.1",
    "sharp": "^0.35.3"
  }
}
